- TTL (time to live) is a superpower of Redis

- the key-value pairs in Redis can have json object as values

- Redis can automatically delete temporary data using TTL (Time To Live), making it ideal for OTPs, verification codes, password reset tokens, temporary sessions, and rate-limiting counters.